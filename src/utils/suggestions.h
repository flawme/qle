#pragma once

#include <string>
#include <vector>

namespace qle {
namespace utils {

std::string SuggestField(const std::string& unknown, const std::vector<std::string>& available);

} // namespace utils
} // namespace qle
